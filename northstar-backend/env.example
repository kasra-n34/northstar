# ─────────────────────────────────────────────
# northstar Backend — Environment Variables
# ─────────────────────────────────────────────
# 1. Copy this file:  cp .env.example .env
# 2. Paste your key below (get it from https://console.anthropic.com)
# 3. NEVER commit .env to git — it's already in .gitignore

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: change the port the backend runs on (default 3001)
# PORT=3001
